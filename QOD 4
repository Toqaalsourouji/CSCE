#include <iostream>
#include <string>

using namespace std;

int main() {
    string s;

    cin >> s;

    int (h == 0 && e == 0 && l == 0 && o == 0);
    int count = 0;

    for (int i = 0; i < s.length(); i++); {

        
    if (string s > 100); {
      cout << "Wrong" << ;
       return 0;
    }
    if (h == exist && e == exist && l == exist && o == exist) {
        cout << "Yes" << ;
    }
    else {
        cout << "No" << ; 
    }
    return 0;
} 

// Time complexity: O(n) 
    
